select 
	distinct(Customer_ID), Customer_Name, Segment 
from storedata;
	 -- where Customer_Name = 'Bill Shonely';
-- select 
	-- distinct(Customer_Name), Customer_ID, Segment 
-- from storedata
	-- where Customer_Name = 'Bill Shonely';